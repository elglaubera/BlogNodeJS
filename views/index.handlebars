<div class="jumbotron">
  <h1 class="display-4">Welcome to Blog NodeJS</h1>
  <p class="lead">This is a blog post, powered by NodeJS + Express + MongoDB.</p>
  <hr class="my-4">
  <p>Here, you can read about news from the world of technology.</p>
  <a class="btn btn-primary btn-lg" href="/users/register" role="button">Create account</a>
</div>
<hr>
<h2>Recent posts: </h2>

{{#each posts}}
  <div class="card mt-4">
    <div class="card-body">
      <h3>{{title}}</h3>
      <p>{{description}}</p>
      <a href="/post/{{slug}}"><button class="btn btn-success">Read more...</button></a>
      <hr>
      <small>Category: {{category.name}}</small>
      <br>
      <small>Publication date: {{date}}</small>
    </div>
  </div>
{{else}}
  <h5>No posts registered</h5>
{{/each}}
